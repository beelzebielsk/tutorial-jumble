/* - This contains a history object.
 * - It seems that they use the history object to change the state of
 *   BrowserRouter, and thus force a re-render.
 * * How do we force re-renders for Routes, too?
 */
